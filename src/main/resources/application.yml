server:
  port: 8082

spring:
  application:
    name: identity-access-service
  datasource:
    url: jdbc:postgresql://localhost:5433/iam
    username: iam
    password: iam
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true

management:
  endpoints:
    web:
      exposure:
        include: [health, info]
